import React from 'react';
import AppointmentReason from './AppointmentReason';

export default {
    title: 'Motivo de consulta',
    component: AppointmentReason,
};

export const Default = () => <AppointmentReason />;
